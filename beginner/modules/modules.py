import converters # Syntax for importing converters module  
from converters import kg_to_lbs # We can import like this so
# we won't need to reference "converters" when we call the 
# function kg_to_lbs

print(converters.kg_to_lbs(70))
kg_to_lbs(100)
