# This a module called converters that we keep stored here.
# If we ever need these functions we can import this file
# into places we need them. 
def lbs_to_kg(weight):
    return weight * 0.45

def kg_to_lbs(weight):
    return weight / 0.45
